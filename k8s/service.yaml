apiVersion: v1
kind: Service
metadata:
  labels:
    app: starbucks-app
  name: starbucks-app
  namespace: starbucks
spec:
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: starbucks-app
  type: NodePort

